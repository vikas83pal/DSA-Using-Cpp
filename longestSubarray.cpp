#include<bits/stdc++.h>
using namespace std;
int longestSubarrayLength(int arr[],int n,int sum){
    int len = 0;
    for(int i=0;i<n;i++){
        for(int)
    }
}
int main(){
    int n;
    cin>> n;
    int arr[n];
    for(int i=0;i<n;i++){
        cin >> arr[i];

    }
    int sum = 3;
    int res = longestSubarrayLength(arr,n,sum);
    cout << res;
    return 0;
}